/*form validation */
/*import { SubmissionError } from 'redux-form'

const sleep = ms => {
  return new Promise((resolve, rejected) => {
    setTimeout(resolve, ms);
  });
}

export const submit = (values) => {
  return sleep(1000).then(() => {
    if (values.lastname == undefined & values.name == undefined & values.subdivisions_id == undefined) {
      throw new SubmissionError({
        _error: 'Должно быть заполнено хотя бы одно поле!'
      });
    } else {
      console.log(values);
    }
  });
}*/

